
### Engine 6cd832a — DOM safe for one writer + concurrent readers (2026-09-24, merged by coordinator)

Per-node `Mutex` covering parent/children/attributes/text/id/class tokens/selector entry (getters return COW snapshots; `hasClass`/`idAttribute`/`attributeValue` look up in place), `lowercasedTagName` fixed at creation, selector epoch an `Atomic<UInt64>` (release/acquire; an older entry never replaces a newer one; the ancestor filter lives in the entry so the Bloom test no longer loads the parent), `:has()` memo per pass thread and cleared when the epoch moves, parser appends text in place. Package platforms raised to iOS 18 / macOS 15 (the app compiles the engine sources itself at iOS 26.2). 5 s stress test (writer + matching reader + `querySelectorAll` reader + detached SVG builder) clean under TSan on the simulator; negative control on e484d5c races immediately. Selector bench slightly faster (apple 30.3 → 27 ms, wiki 173 → 143 ms); kernels within 1 % once code placement is pinned. Probes 486/0; conformance 3313/0 (`DOMThreadSafety`, 10). Submodule bumped, bundle regenerated. **App checklist before `RN_STYLE_ASYNC_CASCADE`/async layout can be turned on:** record `DOMNode.selectorEpoch` at pass start and re-run if it moved (torn trees); queue mutation records/invalidation sets that arrive mid-pass; the style/layout threads must never call into the JS bridge (`nodeDocument`, wrappers, `scriptAlreadyStarted`); exactly one DOM writer (PseudoElementStore on the pass thread is fine; image-decode SVG builders must stay on detached nodes or hop to main); lock `CSSValueParser.safeAreaInsets`; keep `beginStylePass` on the cascade thread. Trap: `swift test -c release` rebuilds `jeffjs-cli` with testability on — rebuild the CLI before benchmarking.
